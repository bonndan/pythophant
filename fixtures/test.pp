<?php
/**
 * Test
 * 
 * a long description: just a simple class with one private variable and two 
 * methods.
 * 
 * @author Daniel Pozzi <bonndan76@googlemail.com> 
 */
class Test
extends StdClass
implements Countable

    /**
     * const test
     * @var string
     */
    const MY_CONST = 'test'

    /**
     * private someVar
     * @var boolean 
     */
    private someVar = false

    /**
     * count function
     * 
     * @return string
     */
    count
        switch @someVar
            case true:
                if not 'SomeClass' class_exists? or 'MY_CONST' defined?
                    result = @someVar explode(':')!
                return 'myString' strtolower()! ucfirst()!
                break
            case false:
                echo 'nothing'
                break

        return @someVar?true:false

    /**
     * function to test if something is contained in an array
     * 
     * @param array something
     * 
     * @return boolean
     */
    private isSomethingContained:
        something = (bool)something
        return something in(['a', 1]) or something ctype_alnum?
    
